import 'package:flutter/material.dart';

class VTextField extends StatelessWidget {
  const VTextField({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
